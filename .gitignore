/.quarto/
/_site/
/_freeze/

.DS_Store
.Rproj.user/
.Rhistory
.RData
.Ruserdata

__pycache__/
*.pyc
.ipynb_checkpoints/

.env
.env.local

# Local Claude Code settings (not part of the site)
.claude/

**/*.quarto_ipynb

# Book sources live locally under books/<slug>-src/ but are not committed.
# Only the rendered HTML output (at books/<slug>/) is pushed.
/books/*-src/

# Slide sources live locally under slides/<slug>-src/ but are not committed.
# Only the rendered HTML output (at slides/<slug>/) is pushed.
/slides/*-src/

draft.qmd
*.pptx